{
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node",
        "config": {
          "maxLambdaSize": "50mb",
          "buildCommand": "npx prisma generate && npm run build"
        }
      }
    ]
  }